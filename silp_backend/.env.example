# ===========================================
# SILP Backend - Environment Variables
# Sistema de Integración de Líderes Privada
# ===========================================
#
# INSTRUCCIONES:
# 1. Copia este archivo a .env: cp .env.example .env
# 2. Edita .env con tus credenciales reales
# 3. NUNCA subas .env al repositorio (está en .gitignore)
#
# ===========================================

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_base_datos
# Ejemplo local: postgresql://postgres:password@localhost:5432/db_provida_uf
# Ejemplo producción: postgresql://user:pass@db.example.com:5432/silp_prod
DATABASE_URL=postgresql://usuario:contraseña@localhost:5432/db_provida_uf

# ===========================================
# SECURITY & JWT
# ===========================================
# IMPORTANTE: Cambia SECRET_KEY en producción por una clave segura y única
# Genera una clave segura: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=tu-clave-secreta-super-segura-cambiala-en-produccion-min-32-caracteres

# Algoritmo para JWT (HS256 es el estándar)
ALGORITHM=HS256

# Tiempo de expiración del token de acceso (en minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Tiempo de expiración del token de recuperación de contraseña (en minutos)
PASSWORD_RESET_TOKEN_EXPIRE_MINUTES=60

# ===========================================
# EMAIL CONFIGURATION (SMTP)
# ===========================================
# Para desarrollo, usa Mailtrap: https://mailtrap.io (gratis)
# Para producción, usa tu servicio SMTP real (Gmail, SendGrid, AWS SES, etc.)

# Credenciales SMTP
MAIL_USERNAME=tu-usuario-mailtrap
MAIL_PASSWORD=tu-contraseña-mailtrap

# Configuración del remitente
MAIL_FROM=noreply@silp.com
MAIL_FROM_NAME=SILP Sistema

# Configuración del servidor SMTP
# Mailtrap: smtp.mailtrap.io
# Gmail: smtp.gmail.com
# SendGrid: smtp.sendgrid.net
MAIL_SERVER=smtp.mailtrap.io
MAIL_PORT=587

# Seguridad del servidor SMTP
MAIL_STARTTLS=True
MAIL_SSL_TLS=False
USE_CREDENTIALS=True
VALIDATE_CERTS=True

# ===========================================
# CORS ORIGINS (separados por comas)
# ===========================================
# URLs permitidas para hacer peticiones CORS
# Desarrollo: http://localhost:5173,http://localhost:3000
# Producción: https://tudominio.com,https://www.tudominio.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ===========================================
# APPLICATION SETTINGS
# ===========================================
APP_NAME=SILP
APP_VERSION=1.0.0

# Modo debug (True para desarrollo, False para producción)
DEBUG=True

# ===========================================
# FRONTEND URL (para links en emails)
# ===========================================
# URL del frontend para incluir en correos (recuperación de contraseña, etc.)
# Desarrollo: http://localhost:5173
# Producción: https://tudominio.com
FRONTEND_URL=http://localhost:5173
